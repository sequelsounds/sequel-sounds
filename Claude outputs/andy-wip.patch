diff --git a/src/components/staff/NewSong.tsx b/src/components/staff/NewSong.tsx
index 0712bbc..3caaab0 100644
--- a/src/components/staff/NewSong.tsx
+++ b/src/components/staff/NewSong.tsx
@@ -82,7 +82,7 @@ export function NewSongModal({ projectId, onClose }: { projectId: number; onClos
 
       {stage === 'pick' && (
         <>
-          <div className="rm-header">Select Composition Team</div>
+          <div className="rm-header">Start New Song Registration</div>
           <div className="rm-subheader">
             Choose the rights Sequel is acquiring, then select the composition team to send a
             Schedule A.
diff --git a/src/routes/SongConfirmation.tsx b/src/routes/SongConfirmation.tsx
index b6055b6..b990608 100644
--- a/src/routes/SongConfirmation.tsx
+++ b/src/routes/SongConfirmation.tsx
@@ -456,6 +456,7 @@ export default function SongConfirmation() {
         {screen === 'form' && step === summaryStep && n > 0 && (
           <div className="sc-state">
             <h2 className="sc-heading">Check everything looks right</h2>
+            <div className="sc-summary-label">Track title</div>
             <p className="sc-summary-title">{title.trim()}</p>
             <div className="sc-writers">
               <div className="sc-writer-header">
@@ -470,8 +471,11 @@ export default function SongConfirmation() {
                   <div className="sc-writer-share">{formatTotal(parseFloat(c.share_split) || 0)}%</div>
                 </div>
               ))}
+              <div className="sc-writer-total">
+                <div className="sc-writer-name">Total</div>
+                <div className="sc-writer-share">{formatTotal(total)}%</div>
+              </div>
             </div>
-            <div className="sc-total">The share total is {formatTotal(total)}%</div>
             {Math.abs(total - 100) > 0.001 && (
               <p className="sc-warning">
                 Typically, this value should equal 100%. Please double check before submitting.
diff --git a/src/styles/design-system.css b/src/styles/design-system.css
index 446c6a7..2a53fce 100644
--- a/src/styles/design-system.css
+++ b/src/styles/design-system.css
@@ -4527,63 +4527,88 @@
     width: 100%;
   }
 
+  /* The summary, tidied (Andy, 16 Sep): the title gets a label like the
+     table's, the header reads as one muted line, the rows lose their rules,
+     and the total sits in the share column instead of a sentence below. */
+  .sc-summary-label,
+  .sc-writer-header {
+    font-size: 0.75rem;
+    letter-spacing: 0.06em;
+    text-transform: uppercase;
+    color: rgba(55, 43, 41, 0.55);
+  }
+
+  .sc-summary-label {
+    margin-top: 1.5rem;
+  }
+
   .sc-summary-title {
-    margin-top: 1rem;
-    margin-bottom: 0.75rem;
+    margin-top: 0.35rem;
+    margin-bottom: 1.75rem;
     font-family: var(--font-sans);
-    font-size: 1.1rem;
-    font-weight: 300;
+    font-size: 1.45rem;
+    font-weight: 400;
+    line-height: 1.25;
   }
 
   .sc-writers {
     width: 100%;
     margin-bottom: 2rem;
-    border-top: 1px solid rgba(55, 43, 41, 0.15);
+    border-top: 1px solid rgba(55, 43, 41, 0.2);
   }
 
   .sc-writer-header {
     display: flex;
     justify-content: space-between;
-    padding-top: 0.75rem;
-    padding-bottom: 0.4rem;
-    font-size: 0.7rem;
-    letter-spacing: 0.02em;
-    text-transform: uppercase;
-    opacity: 0.5;
+    padding-top: 1rem;
+    padding-bottom: 0.5rem;
   }
 
-  .sc-writer-row {
+  .sc-writer-row,
+  .sc-writer-total {
     display: flex;
     align-items: baseline;
     justify-content: space-between;
-    padding-top: 0.65rem;
-    padding-bottom: 0.65rem;
-    border-bottom: 1px solid rgba(55, 43, 41, 0.1);
+    padding-top: 0.5rem;
+    padding-bottom: 0.5rem;
+  }
+
+  .sc-writer-total {
+    margin-top: 0.75rem;
   }
 
   .sc-writer-name {
     flex-grow: 1;
+    min-width: 0;
     padding-right: 1rem;
-    font-weight: 400;
+    overflow-wrap: anywhere;
   }
 
   .sc-writer-cae {
     flex-shrink: 0;
-    width: 8rem;
-    opacity: 0.7;
+    width: 9rem;
+    overflow-wrap: anywhere;
+  }
+
+  .sc-writer-row .sc-writer-cae {
+    color: rgba(55, 43, 41, 0.7);
   }
 
   .sc-writer-share {
     flex-shrink: 0;
-    width: 4rem;
-    font-weight: 500;
+    width: 4.5rem;
     text-align: right;
   }
 
-  .sc-total {
-    margin-bottom: 0.5rem;
-    font-size: var(--sc-lg);
-    font-weight: 400;
+  .sc-writer-total .sc-writer-name,
+  .sc-writer-total .sc-writer-share {
+    font-weight: 500;
+  }
+
+  @media screen and (max-width: 478px) {
+    .sc-writer-cae {
+      width: 6.5rem;
+    }
   }
 
   /* New (Andy, 16 Sep): the signing step. The old page has no equivalent, so
